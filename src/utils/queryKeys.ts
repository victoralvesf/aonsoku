const playlist = {
  all: 'get-all-playlists',
}

const album = {
  all: 'get-all-albums',
}

const artist = {
  all: 'get-all-artists',
}

const song = {
  all: 'get-all-songs',
}

export const queryKeys = {
  album,
  artist,
  playlist,
  song,
}
